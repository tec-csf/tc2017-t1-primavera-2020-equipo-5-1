dist: trusty
sudo: required
language:
  - cpp
compiler:
  - g++
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-7
      - cmake
script:
  - g++ test.cpp 
  - ./a.out -i test3.cpp
